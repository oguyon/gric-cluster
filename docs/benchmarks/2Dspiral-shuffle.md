# Shuffled Points on 2D Spiral

**Category**: 2D Trajectories  
**Data Type**: `txt` (2,000 frames)  
**Clustering Parameter**: `rlim = 0.10`

---

## Scenario Overview

Points randomly sampled from a multi-arm spiral manifold with order shuffled. Stresses
geometric metric pruning on non-convex geometric manifolds.

## Visual Diagnostics (`gric-plot`)

Below is the visualization generated by `gric-plot` showing the sample manifold,
cluster centroids with radius threshold circles ($r_{\text{lim}}$),
distance call distribution, and cluster size histogram:

![Shuffled Points on 2D Spiral Cluster Plot](images/2Dspiral-shuffle.png)

### Query & Candidate Ranking Diagnostics

![Shuffled Points on 2D Spiral Query Diagnostics](images/2Dspiral-shuffle.queries.png)

## Execution Commands

### 1. Data Generation
```bash
gric-mktxtseq \
    2000 \
    2Dspiral-shuffle.txt \
    2Dspiral \
    -shuffle
```

### 2. Clustering Execution
```bash
gric-cluster \
    0.10 \
    -maxcl \
    2500 \
    -maxim \
    2000 \
    -outdir \
    out_2Dspiral-shuffle \
    -clustered \
    2Dspiral-shuffle.txt
```

### 3. Diagnostic Visualization
```bash
gric-plot 2Dspiral-shuffle.txt \
    out_2Dspiral-shuffle/cluster_run.log \
    docs/benchmarks/images/2Dspiral-shuffle.png
```

## Performance Measurements

| Metric | Measured Value | Description |
| :--- | :--- | :--- |
| **Total Frames** | `2,000` | Number of sequential frames processed |
| **Execution Time** | `69.894 ms` | Total wall-clock runtime |
| **Throughput** | `28,614 fps` | Frames processed per second |
| **Active Clusters / States** | `48` | Total distinct clusters created |
| **Total Distance Calls ($d$)** | `6,647` | All distance calls ($d_S + d_C$) |
| **Sample Distances ($d_S$)** | `5,519` | Sample-to-cluster evaluations |
| **$d_S / \text{frame}$** | `**2.76**` | Search calls per frame |
| **Total $d / \text{frame}$** | `**3.32**` | Total distance ops per frame |
| **Peak Memory** | `135,000 KB` | Peak resident set size (RSS) |

---

## Algorithmic Insights

Metric distance geometry efficiently separates nested spiral arms despite lack of temporal
locality. Pruning reduces candidate evaluations from 48 to **~2.8 distance evaluations per
frame**.

---

[← Back to Benchmarks Overview](index.md)
